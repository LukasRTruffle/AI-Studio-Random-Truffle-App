import React from 'react';
import SetupCard from '../../components/setup/SetupCard';

const SetupActivation: React.FC = () => {
  return (
    <SetupCard title="Activation Platforms">
      <p>Configure and test connections to activation platforms like Google Ads, DV360, etc.</p>
    </SetupCard>
  );
};

export default SetupActivation;
