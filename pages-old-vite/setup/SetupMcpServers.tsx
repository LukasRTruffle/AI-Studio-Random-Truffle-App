import React from 'react';
import SetupCard from '../../components/setup/SetupCard';

const SetupMcpServers: React.FC = () => {
  return (
    <SetupCard title="MCP Servers">
      <p>Configure and test Marketing Cloud Platform (MCP) servers.</p>
    </SetupCard>
  );
};

export default SetupMcpServers;
